# Partner API token for the demo partner (TEST environment).
# Never commit the real value — .env is gitignored.
VMC_DEMO_TOKEN=vmc_test_REPLACE_ME

# Optional overrides (defaults shown):
# VMC_API_BASE=https://test.verifymyclient.com/api/v1
# DEMO_PORT=4100
